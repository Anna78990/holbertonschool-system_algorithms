#include <stdlib.h>
#include <stdio.h>

#include "graphs.h"

/**
 * main - Entry point
 *
 * Return: EXIT_SUCCESS or EXIT_FAILURE
 */
int main(void)
{
	char *hello = "hello";
	int s;

	s = strcmp(hello, "hello ");

	printf("%d\n", s);
	
	if (hello = "hello ")
		printf("succes");

	return (EXIT_SUCCESS);
}
